<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.SET_ALARM"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dentex"
        tools:targetApi="31">
        <receiver android:name=".Appointments.MessageBroadcast" android:enabled="true" android:exported="false"/>
        <activity
            android:name=".Entry.Login"
            android:exported="false" />
        <activity
            android:name=".Entry.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".Clinic"
            android:exported="false" />
        <activity
            android:name=".Doctor"
            android:exported="false" />
        <activity
            android:name=".Patient.PatientActivity"
            android:exported="false" />
        <activity
            android:name=".Entry.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>